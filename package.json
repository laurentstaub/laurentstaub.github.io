{
  "name": "antheos-data-site",
  "version": "1.0.0",
  "description": "Site Antheos Data — générateur d'articles minimaliste (Markdown → HTML statique).",
  "private": true,
  "scripts": {
    "build": "node build/build.js",
    "migrate": "node build/migrate.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
